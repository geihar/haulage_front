import React from 'react'
import Profile from '../../Profile'

const PassSec = () => (
  <Profile>
    <div>
      <h1> Password & Security page</h1>
    </div>
  </Profile>

)

export default PassSec
